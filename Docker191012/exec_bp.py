import os
import sys


try:
	command = sys.argv[1]
	#sorry,i hide thee code of mend the passwd in the getpasswd.php
	os.system(command)
	#i mend the passwd again,so what you look are wrong passwd
except:
	pass
